/// @description Example of a script used with game_camera_direct. The script must have the one argument provided, but can otherwise do just about anything.
/// @argument {real} vind view index
function game_camera_default_behavior(argument0) {
	var vind = argument0;

	view_follow(vind, x, y);



}
