function player_is_linked(argument0) {
	// player_is_linked(step)

	var phase = argument0;

	switch (phase) {
	case "start":
	    // depends on the linked object
	    break;

	case "finish":
	    linked_object_id = noone;
	    break;

	case "step":
	    // depends on the linked object
	    break;
	}



}
